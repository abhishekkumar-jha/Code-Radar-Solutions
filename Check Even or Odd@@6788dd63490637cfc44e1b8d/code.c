#include<stdio.h>
int main() {
    int number;
    printf("");
    scanf("%d", &number);
    if(number % 2 == 0) {
        printf("%d", number);
    } else{
        printf("%d", number);
    }
    return 0;
}