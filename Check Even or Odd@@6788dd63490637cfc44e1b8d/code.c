#include<stdio.h>
int main() {
    int number;
    printf("Integer ");
    scanf("%d", &number);
    if(n % 2 == 0) {
        printf("%d even", n);
    } else{
        printf("%d odd", n);
    }
    return 0;
}