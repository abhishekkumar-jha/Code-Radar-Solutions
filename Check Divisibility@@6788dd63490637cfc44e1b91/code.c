#include<stdio.h>
int main() {
    int num;
    scanf("%num", &num);
    if(num / 5,11) {
        printf("Divisible\n");
    }
    else {
        printf("Not Divisible\n");
    }
    return 0;
}